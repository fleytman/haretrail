# Scripts

Reusable setup and maintenance scripts will live here.

Status: Phase 3 migration target.

Planned scripts:

- connector installer;
- setup validator;
- path/config checker;
- smoke tests for sample data.

Scripts should use explicit configuration such as `HARETRAIL_DATA_DIR` when they need access to a data repository.
